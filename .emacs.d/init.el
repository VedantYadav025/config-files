;; Disable the ring bell feature
(setq ring-bell-function 'ignore)
(setq visible-bell t)

;; Set the type of line numbering to relative
(setq display-line-numbers-type 'relative) 

;; Activate the line numbers globally in all buffers
(global-display-line-numbers-mode)
